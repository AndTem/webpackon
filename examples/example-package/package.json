{
  "name": "@webpackon/example-package",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "yarn -v",
    "build:prod": "webpack --mode=production --config webpack.config.js",
    "dev": "webpack serve --mode=development",
    "lint:types": "yarn -v",
    "release": "yarn -v"
  }
}
